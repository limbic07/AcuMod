## 项目描述
本项目Acumen MOD manager是一款怪猎世界（MHW）的mod管理器，集成传统管理mod的功能以及AI Agent功能，其中AI功能用于语言控制mod管理功能（例如启停、卸载某mod）、智能下载安装mod。

## 技术栈

本项目是 Tauri 桌面应用：

- 桌面框架：Tauri 2。
- 前端：Vue 3 + TypeScript + Vite，包管理和脚本命令使用 npm。
- 后端：Rust 2021 edition，Tauri command 作为前后端边界。
- 前后端通信：前端通过 `@tauri-apps/api/core` 的 `invoke()` 调用 Rust `#[tauri::command]`。
- 序列化：Rust 使用 `serde` / `serde_json` 与前端交换结构化 DTO。
- 架构方向：先实现可靠的传统 MOD 管理器，再把 AI Agent 接入同一套受控操作入口；AI 不应直接操作文件系统。

## 文档阅读顺序

项目文档放在 `docs/` 下，保持少而清楚。开始较大改动前，先阅读与任务相关的文档；如果对应文档尚未创建，优先补齐最小可用内容。

建议阅读顺序：

1. `docs/README.md`：文档入口、当前阶段、下一步计划。
2. `docs/architecture.md`：前端、Tauri 命令、Rust 分层、AI 接入边界。
3. `docs/features.md`：传统 MOD 管理、Profile、下载、AI Agent 的功能拆分。
4. `docs/development.md`：开发命令、目录约定、Tauri invoke 检查清单。
5. `docs/security.md`：文件操作安全、用户确认机制、AI 执行边界。

## 学习型协作约定

本项目也是一个学习型项目。修改代码时，不只追求功能完成，也要让项目结构、数据流和实现原因容易理解。

1. 修改前先简要说明本次目标、涉及文件、实现思路和预计验证方式。

2. 实现时优先选择清晰、直白、可读的代码；命名要表达职责，避免为了“高级”而引入不必要抽象。

3. 注释用于解释为什么这样设计、模块边界、框架机制或非显而易见的行为；不要写重复代码含义的空泛注释。

4. 修改后必须说明：
   - 改了哪些文件；
   - 为什么这样改；
   - 关键数据流或调用链如何工作；
   - 运行了哪些验证命令，结果如何。

5. 遇到架构选择、依赖选择、文件操作安全、AI Agent 执行边界等问题时，先说明取舍，再进行实现。

6. 优先交付薄的端到端切片，例如 `Vue UI -> typed invoke wrapper -> Tauri command -> Rust service -> DTO response`，确认链路稳定后再扩展复杂功能。

## Git 提交约定

1. 每完成一个可验证的 slice，先运行 `git status`，说明本次建议提交的文件范围和提交信息。
2. 未经用户明确同意，不主动执行 `git add`、`git commit` 或改写历史。
3. 用户确认提交后，优先按 slice 维度提交，提交信息使用简洁中文或英文，例如 `feat: 完成游戏目录检测切片`。

## 硬性规则

1. 修改代码前必须先阅读相关文件，不允许凭空猜测项目结构、函数名、字段名或命令名。

2. 每次修改必须尽量保持最小改动，不允许无关重构、无关格式化、无关依赖升级。

3. 不允许擅自删除用户文件、清空数据、删除目录，禁止执行 `rm -rf`、批量删除、清库等破坏性操作。

4. 不允许擅自新增生产依赖；如确实需要新增依赖，必须先说明原因、用途、替代方案和影响。

5. 修改前端调用 Tauri/Rust 命令时，必须同步检查：
   - 前端 `invoke()` 命令名
   - Rust `#[tauri::command]`
   - `invoke_handler` 注册
   - 参数名和参数类型

6. 修改 Rust 代码后，必须优先运行或建议运行：
   - `cd src-tauri && cargo check`
   - `cd src-tauri && cargo fmt`
   如果失败，必须原样说明错误原因，不允许假装通过。

7. 修改 Vue/TypeScript 代码后，必须优先运行或建议运行：
   - `npm run dev`
   - 项目存在时运行 `npm run build`
   如果失败，必须说明失败位置和下一步处理方案。

8. 最终回复必须包含：
   - 修改了哪些文件
   - 为什么这样改
   - 预期实现哪些功能，如何验证
   - 还有什么风险或未完成项
